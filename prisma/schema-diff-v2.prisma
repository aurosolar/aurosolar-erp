// ═══════════════════════════════════════════════════════════
// CAMBIOS EN schema.prisma — Gate Engine v2
// Aplicar estos diffs sobre el schema existente
// ═══════════════════════════════════════════════════════════

// ── DIFF 1: Enum EstadoObra — ELIMINAR TERMINADA ──
// Quitar TERMINADA. Flujo: REVISION_COORDINADOR → LEGALIZACION directo.

// ── DIFF 2: Nuevo enum ──
// enum EstadoChecklist { BORRADOR SUBMITIDA APROBADA RECHAZADA }

// ── DIFF 3: ChecklistValidacion — añadir campos ──
//   status          EstadoChecklist  @default(BORRADOR)
//   submittedAt     DateTime?        @map("submitted_at")
//   submittedById   String?          @map("submitted_by_id")
//   reviewedAt      DateTime?        @map("reviewed_at")
//   reviewedById    String?          @map("reviewed_by_id")
//   reviewDecision  String?          @map("review_decision")
//   reviewNotes     String?          @map("review_notes")
//   submittedBy     Usuario?         @relation("ChecklistSubmitter", ...)
//   reviewedBy      Usuario?         @relation("ChecklistReviewer", ...)
//   @@index([obraId, status])  ← reemplazar @@index([obraId])

// ── DIFF 4: PlanPago — añadir campo ──
//   requiereParaEstado  String?  @map("requiere_para_estado")

// ── DIFF 5: Usuario — añadir relaciones ──
//   checklistSubmitidos  ChecklistValidacion[]  @relation("ChecklistSubmitter")
//   checklistRevisados   ChecklistValidacion[]  @relation("ChecklistReviewer")
